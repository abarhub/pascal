#ifndef _DEBOGAGE_H_

#include<time.h>

#define DEBUT(X)  { int (X)[2]; (X)[0]=clock();
#define FIN(X,Y)  (X)[1]=clock();printf(Y);printf(" %.04lf secondes\n",(((X)[1]-(X)[0])/CLOCKS_PER_SEC)); }

#endif